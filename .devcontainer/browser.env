PUID=1000
PGID=1000
TZ=Asia/Shanghai
CUSTOM_PORT=6657
CUSTOM_HTTPS_PORT=6658
TITLE=Harness Kanban Workspace Browser
# Basic auth is recommended because the GUI grants shell access inside the browser container.
CUSTOM_USER=workspace
PASSWORD=workspace
CHROME_CLI=--disable-dev-shm-usage --no-first-run --no-default-browser-check --remote-debugging-port=9222 http://localhost:3000
